let config = {
    host:            'localhost',
    port:            3306,
    database:        'mydal_test',
    user:            'mydal_test',
    password:        'mydal_test',
    connectionLimit: 10,
    dateStrings:     true
};

export = config;
